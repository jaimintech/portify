import SwiftUI

struct GeneralSettingsView: View {
    var body: some View {
        Form {
            Text("Settings coming soon")
                .foregroundColor(.secondary)
        }
        .padding()
    }
}
